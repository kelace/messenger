export default class User {
  constructor(id, name, relationInitiator, status) {
    this.id = id;
    this.name = name;
    this.relationInitiator = relationInitiator;
    this.status = status;
  }
}
